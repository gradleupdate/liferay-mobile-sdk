include 'android', 'builder', 'ios', 'windows'

def modules = files { file('modules').listFiles() }

modules.each { File module ->
	if (module.isDirectory() && !module.isHidden()) {
		include "modules/${module.name}"
	}
}